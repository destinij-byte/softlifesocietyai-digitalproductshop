import React from "react";

import { PlaceholderScreen } from "../components/PlaceholderScreen";

export function RoutinesScreen() {
  return (
    <PlaceholderScreen
      emoji="☀️"
      title="Routines"
      note="Customizable Morning and Night checklists, with her own steps added on top of the defaults."
    />
  );
}
