import React from "react";

import { PlaceholderScreen } from "../components/PlaceholderScreen";

export function NourishAIScreen() {
  return (
    <PlaceholderScreen
      emoji="🍽️"
      title="Nourish AI"
      note="Calorie ring, macro bars, 3 ways to log food (photo, search, manual), AI meal builder."
    />
  );
}
