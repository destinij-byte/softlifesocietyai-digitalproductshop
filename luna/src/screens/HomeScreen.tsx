import React from "react";

import { PlaceholderScreen } from "../components/PlaceholderScreen";

export function HomeScreen() {
  return (
    <PlaceholderScreen
      emoji="✨"
      title="Home"
      note="Daily command center: greeting, Today's Focus, quick stats, AI suggestion card, Challenges teaser."
    />
  );
}
