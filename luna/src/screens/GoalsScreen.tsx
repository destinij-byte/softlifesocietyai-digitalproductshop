import React from "react";

import { PlaceholderScreen } from "../components/PlaceholderScreen";

export function GoalsScreen() {
  return (
    <PlaceholderScreen
      emoji="🎯"
      title="Goals"
      note="Money, Wellness, Career, Personal - each with a target, deadline, progress bar, and milestones."
    />
  );
}
